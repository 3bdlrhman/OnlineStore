@model IEnumerable<OnlineStore.Models.Category>

@{
    ViewBag.Title = "CategoriesIndex";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<br />
<table class="table table-bordered table-hover">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Details)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Details)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-default" })
                @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-info" })
                @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>
<ul class="pagination">
    <li>
        <a href="/categories/index?page=@ViewBag.previousPage">Previous</a>
    </li>
    <li>
        <a href="/categories/index?page=@ViewBag.nextPage">Next</a>
    </li>
</ul>
<br />
<p>
    @Html.ActionLink("Add New Category", "Create", null, new { @class = "btn btn-primary" })
</p>
